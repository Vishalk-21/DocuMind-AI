# Local development only.
VITE_API_URL=http://localhost:5000

# Vercel Production must use the backend's public HTTP(S) URL, for example:
# VITE_API_URL=https://your-backend-name.onrender.com
# Do not use SMTP_HOST, smtp.gmail.com, or port 587 here.
